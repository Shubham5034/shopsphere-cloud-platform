apiVersion: v1
kind: Service

metadata:
  name: {{ include "shopsphere.fullname" . }}-postgres
  labels:
    app: postgres

spec:
  type: ClusterIP

  selector:
    app: postgres

  ports:
    - protocol: TCP
      port: {{ .Values.postgres.service.port }}
      targetPort: 5432
