apiVersion: v1
kind: ConfigMap

metadata:
  name: {{ include "shopsphere.fullname" . }}-config

data:
  DB_HOST: {{ include "shopsphere.fullname" . }}-postgres
  DB_PORT: {{ .Values.database.port | quote }}
  DB_NAME: {{ .Values.database.name | quote }}
